<ul id="notes" class="list-group" style="list-style-type: none">
  <% company.notes.reverse_each do |note| %>
    <li class="bg-white rounded-3 my-1 py-2 px-3">
      <p><strong><%= note.created_at.strftime("%d %b %Y") %></strong></p>
      <p><%= note.content %></p>
    <li>
  <% end %>
</ul>
